// SPDX-License-Identifier: MIT
// Copyright (C) 2026 Shogo Technologies, Inc.
/**
 * Auto-generated Client Functions
 *
 * Fetch-based CRUD operations that call the Hono REST API.
 * No server-side imports — safe for browser bundles.
 * Generated by `shogo generate` - DO NOT EDIT
 */

import type {
  UserType,
  UserCreateInput,
  UserUpdateInput,
  AdminUserType,
  AdminUserCreateInput,
  AdminUserUpdateInput,
  PaymentSettingsType,
  PaymentSettingsCreateInput,
  PaymentSettingsUpdateInput,
  ServiceType,
  ServiceCreateInput,
  ServiceUpdateInput,
  DepositType,
  DepositCreateInput,
  DepositUpdateInput,
  OrderType,
  OrderCreateInput,
  OrderUpdateInput,
  WalletTransactionType,
  WalletTransactionCreateInput,
  WalletTransactionUpdateInput,
  NotificationType,
  NotificationCreateInput,
  NotificationUpdateInput,
  SupportTicketType,
  SupportTicketCreateInput,
  SupportTicketUpdateInput,
  PlanType,
  PlanCreateInput,
  PlanUpdateInput,
  UserPlanType,
  UserPlanCreateInput,
  UserPlanUpdateInput,
} from './types'

/** Get the API base URL */
function getApiBase(): string {
  if (typeof window !== 'undefined') {
    return window.location.origin
  }
  return process.env.API_URL || 'http://localhost:3001'
}

// ============================================================================
// User Client Functions
// ============================================================================

/**
 * List all User records
 */
export async function getUserList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<UserType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/users${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list User')
  }
  const json = await response.json()
  return (json.items || []) as UserType[]
}

/**
 * Get a single User by ID
 */
export async function getUserById(args: { data: { id: string; userId?: string } }): Promise<UserType> {
  const url = `${getApiBase()}/api/users/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'User not found')
  }
  const json = await response.json()
  return json.data as UserType
}

/**
 * Create a new User
 */
export async function createUser(args: { data: { input: UserCreateInput; userId?: string } }): Promise<UserType> {
  const url = `${getApiBase()}/api/users`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create User')
  }
  const json = await response.json()
  return json.data as UserType
}

/**
 * Update an existing User
 */
export async function updateUser(args: { data: { id: string; input: UserUpdateInput; userId?: string } }): Promise<UserType> {
  const url = `${getApiBase()}/api/users/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update User')
  }
  const json = await response.json()
  return json.data as UserType
}

/**
 * Delete a User
 */
export async function deleteUser(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/users/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete User')
  }
  return { success: true }
}

// ============================================================================
// AdminUser Client Functions
// ============================================================================

/**
 * List all AdminUser records
 */
export async function getAdminUserList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<AdminUserType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/admin-users${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list AdminUser')
  }
  const json = await response.json()
  return (json.items || []) as AdminUserType[]
}

/**
 * Get a single AdminUser by ID
 */
export async function getAdminUserById(args: { data: { id: string; userId?: string } }): Promise<AdminUserType> {
  const url = `${getApiBase()}/api/admin-users/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'AdminUser not found')
  }
  const json = await response.json()
  return json.data as AdminUserType
}

/**
 * Create a new AdminUser
 */
export async function createAdminUser(args: { data: { input: AdminUserCreateInput; userId?: string } }): Promise<AdminUserType> {
  const url = `${getApiBase()}/api/admin-users`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create AdminUser')
  }
  const json = await response.json()
  return json.data as AdminUserType
}

/**
 * Update an existing AdminUser
 */
export async function updateAdminUser(args: { data: { id: string; input: AdminUserUpdateInput; userId?: string } }): Promise<AdminUserType> {
  const url = `${getApiBase()}/api/admin-users/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update AdminUser')
  }
  const json = await response.json()
  return json.data as AdminUserType
}

/**
 * Delete a AdminUser
 */
export async function deleteAdminUser(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/admin-users/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete AdminUser')
  }
  return { success: true }
}

// ============================================================================
// PaymentSettings Client Functions
// ============================================================================

/**
 * List all PaymentSettings records
 */
export async function getPaymentSettingsList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<PaymentSettingsType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/payment-settingses${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list PaymentSettings')
  }
  const json = await response.json()
  return (json.items || []) as PaymentSettingsType[]
}

/**
 * Get a single PaymentSettings by ID
 */
export async function getPaymentSettingsById(args: { data: { id: string; userId?: string } }): Promise<PaymentSettingsType> {
  const url = `${getApiBase()}/api/payment-settingses/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'PaymentSettings not found')
  }
  const json = await response.json()
  return json.data as PaymentSettingsType
}

/**
 * Create a new PaymentSettings
 */
export async function createPaymentSettings(args: { data: { input: PaymentSettingsCreateInput; userId?: string } }): Promise<PaymentSettingsType> {
  const url = `${getApiBase()}/api/payment-settingses`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create PaymentSettings')
  }
  const json = await response.json()
  return json.data as PaymentSettingsType
}

/**
 * Update an existing PaymentSettings
 */
export async function updatePaymentSettings(args: { data: { id: string; input: PaymentSettingsUpdateInput; userId?: string } }): Promise<PaymentSettingsType> {
  const url = `${getApiBase()}/api/payment-settingses/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update PaymentSettings')
  }
  const json = await response.json()
  return json.data as PaymentSettingsType
}

/**
 * Delete a PaymentSettings
 */
export async function deletePaymentSettings(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/payment-settingses/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete PaymentSettings')
  }
  return { success: true }
}

// ============================================================================
// Service Client Functions
// ============================================================================

/**
 * List all Service records
 */
export async function getServiceList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<ServiceType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/services${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list Service')
  }
  const json = await response.json()
  return (json.items || []) as ServiceType[]
}

/**
 * Get a single Service by ID
 */
export async function getServiceById(args: { data: { id: string; userId?: string } }): Promise<ServiceType> {
  const url = `${getApiBase()}/api/services/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Service not found')
  }
  const json = await response.json()
  return json.data as ServiceType
}

/**
 * Create a new Service
 */
export async function createService(args: { data: { input: ServiceCreateInput; userId?: string } }): Promise<ServiceType> {
  const url = `${getApiBase()}/api/services`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create Service')
  }
  const json = await response.json()
  return json.data as ServiceType
}

/**
 * Update an existing Service
 */
export async function updateService(args: { data: { id: string; input: ServiceUpdateInput; userId?: string } }): Promise<ServiceType> {
  const url = `${getApiBase()}/api/services/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update Service')
  }
  const json = await response.json()
  return json.data as ServiceType
}

/**
 * Delete a Service
 */
export async function deleteService(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/services/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete Service')
  }
  return { success: true }
}

// ============================================================================
// Deposit Client Functions
// ============================================================================

/**
 * List all Deposit records
 */
export async function getDepositList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<DepositType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/deposits${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list Deposit')
  }
  const json = await response.json()
  return (json.items || []) as DepositType[]
}

/**
 * Get a single Deposit by ID
 */
export async function getDepositById(args: { data: { id: string; userId?: string } }): Promise<DepositType> {
  const url = `${getApiBase()}/api/deposits/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Deposit not found')
  }
  const json = await response.json()
  return json.data as DepositType
}

/**
 * Create a new Deposit
 */
export async function createDeposit(args: { data: { input: DepositCreateInput; userId?: string } }): Promise<DepositType> {
  const url = `${getApiBase()}/api/deposits`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create Deposit')
  }
  const json = await response.json()
  return json.data as DepositType
}

/**
 * Update an existing Deposit
 */
export async function updateDeposit(args: { data: { id: string; input: DepositUpdateInput; userId?: string } }): Promise<DepositType> {
  const url = `${getApiBase()}/api/deposits/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update Deposit')
  }
  const json = await response.json()
  return json.data as DepositType
}

/**
 * Delete a Deposit
 */
export async function deleteDeposit(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/deposits/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete Deposit')
  }
  return { success: true }
}

// ============================================================================
// Order Client Functions
// ============================================================================

/**
 * List all Order records
 */
export async function getOrderList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<OrderType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/orders${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list Order')
  }
  const json = await response.json()
  return (json.items || []) as OrderType[]
}

/**
 * Get a single Order by ID
 */
export async function getOrderById(args: { data: { id: string; userId?: string } }): Promise<OrderType> {
  const url = `${getApiBase()}/api/orders/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Order not found')
  }
  const json = await response.json()
  return json.data as OrderType
}

/**
 * Create a new Order
 */
export async function createOrder(args: { data: { input: OrderCreateInput; userId?: string } }): Promise<OrderType> {
  const url = `${getApiBase()}/api/orders`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create Order')
  }
  const json = await response.json()
  return json.data as OrderType
}

/**
 * Update an existing Order
 */
export async function updateOrder(args: { data: { id: string; input: OrderUpdateInput; userId?: string } }): Promise<OrderType> {
  const url = `${getApiBase()}/api/orders/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update Order')
  }
  const json = await response.json()
  return json.data as OrderType
}

/**
 * Delete a Order
 */
export async function deleteOrder(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/orders/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete Order')
  }
  return { success: true }
}

// ============================================================================
// WalletTransaction Client Functions
// ============================================================================

/**
 * List all WalletTransaction records
 */
export async function getWalletTransactionList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<WalletTransactionType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/wallet-transactions${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list WalletTransaction')
  }
  const json = await response.json()
  return (json.items || []) as WalletTransactionType[]
}

/**
 * Get a single WalletTransaction by ID
 */
export async function getWalletTransactionById(args: { data: { id: string; userId?: string } }): Promise<WalletTransactionType> {
  const url = `${getApiBase()}/api/wallet-transactions/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'WalletTransaction not found')
  }
  const json = await response.json()
  return json.data as WalletTransactionType
}

/**
 * Create a new WalletTransaction
 */
export async function createWalletTransaction(args: { data: { input: WalletTransactionCreateInput; userId?: string } }): Promise<WalletTransactionType> {
  const url = `${getApiBase()}/api/wallet-transactions`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create WalletTransaction')
  }
  const json = await response.json()
  return json.data as WalletTransactionType
}

/**
 * Update an existing WalletTransaction
 */
export async function updateWalletTransaction(args: { data: { id: string; input: WalletTransactionUpdateInput; userId?: string } }): Promise<WalletTransactionType> {
  const url = `${getApiBase()}/api/wallet-transactions/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update WalletTransaction')
  }
  const json = await response.json()
  return json.data as WalletTransactionType
}

/**
 * Delete a WalletTransaction
 */
export async function deleteWalletTransaction(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/wallet-transactions/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete WalletTransaction')
  }
  return { success: true }
}

// ============================================================================
// Notification Client Functions
// ============================================================================

/**
 * List all Notification records
 */
export async function getNotificationList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<NotificationType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/notifications${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list Notification')
  }
  const json = await response.json()
  return (json.items || []) as NotificationType[]
}

/**
 * Get a single Notification by ID
 */
export async function getNotificationById(args: { data: { id: string; userId?: string } }): Promise<NotificationType> {
  const url = `${getApiBase()}/api/notifications/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Notification not found')
  }
  const json = await response.json()
  return json.data as NotificationType
}

/**
 * Create a new Notification
 */
export async function createNotification(args: { data: { input: NotificationCreateInput; userId?: string } }): Promise<NotificationType> {
  const url = `${getApiBase()}/api/notifications`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create Notification')
  }
  const json = await response.json()
  return json.data as NotificationType
}

/**
 * Update an existing Notification
 */
export async function updateNotification(args: { data: { id: string; input: NotificationUpdateInput; userId?: string } }): Promise<NotificationType> {
  const url = `${getApiBase()}/api/notifications/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update Notification')
  }
  const json = await response.json()
  return json.data as NotificationType
}

/**
 * Delete a Notification
 */
export async function deleteNotification(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/notifications/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete Notification')
  }
  return { success: true }
}

// ============================================================================
// SupportTicket Client Functions
// ============================================================================

/**
 * List all SupportTicket records
 */
export async function getSupportTicketList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<SupportTicketType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/support-tickets${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list SupportTicket')
  }
  const json = await response.json()
  return (json.items || []) as SupportTicketType[]
}

/**
 * Get a single SupportTicket by ID
 */
export async function getSupportTicketById(args: { data: { id: string; userId?: string } }): Promise<SupportTicketType> {
  const url = `${getApiBase()}/api/support-tickets/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'SupportTicket not found')
  }
  const json = await response.json()
  return json.data as SupportTicketType
}

/**
 * Create a new SupportTicket
 */
export async function createSupportTicket(args: { data: { input: SupportTicketCreateInput; userId?: string } }): Promise<SupportTicketType> {
  const url = `${getApiBase()}/api/support-tickets`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create SupportTicket')
  }
  const json = await response.json()
  return json.data as SupportTicketType
}

/**
 * Update an existing SupportTicket
 */
export async function updateSupportTicket(args: { data: { id: string; input: SupportTicketUpdateInput; userId?: string } }): Promise<SupportTicketType> {
  const url = `${getApiBase()}/api/support-tickets/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update SupportTicket')
  }
  const json = await response.json()
  return json.data as SupportTicketType
}

/**
 * Delete a SupportTicket
 */
export async function deleteSupportTicket(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/support-tickets/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete SupportTicket')
  }
  return { success: true }
}

// ============================================================================
// Plan Client Functions
// ============================================================================

/**
 * List all Plan records
 */
export async function getPlanList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<PlanType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/plans${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list Plan')
  }
  const json = await response.json()
  return (json.items || []) as PlanType[]
}

/**
 * Get a single Plan by ID
 */
export async function getPlanById(args: { data: { id: string; userId?: string } }): Promise<PlanType> {
  const url = `${getApiBase()}/api/plans/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Plan not found')
  }
  const json = await response.json()
  return json.data as PlanType
}

/**
 * Create a new Plan
 */
export async function createPlan(args: { data: { input: PlanCreateInput; userId?: string } }): Promise<PlanType> {
  const url = `${getApiBase()}/api/plans`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create Plan')
  }
  const json = await response.json()
  return json.data as PlanType
}

/**
 * Update an existing Plan
 */
export async function updatePlan(args: { data: { id: string; input: PlanUpdateInput; userId?: string } }): Promise<PlanType> {
  const url = `${getApiBase()}/api/plans/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update Plan')
  }
  const json = await response.json()
  return json.data as PlanType
}

/**
 * Delete a Plan
 */
export async function deletePlan(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/plans/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete Plan')
  }
  return { success: true }
}

// ============================================================================
// UserPlan Client Functions
// ============================================================================

/**
 * List all UserPlan records
 */
export async function getUserPlanList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<UserPlanType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/user-plans${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list UserPlan')
  }
  const json = await response.json()
  return (json.items || []) as UserPlanType[]
}

/**
 * Get a single UserPlan by ID
 */
export async function getUserPlanById(args: { data: { id: string; userId?: string } }): Promise<UserPlanType> {
  const url = `${getApiBase()}/api/user-plans/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'UserPlan not found')
  }
  const json = await response.json()
  return json.data as UserPlanType
}

/**
 * Create a new UserPlan
 */
export async function createUserPlan(args: { data: { input: UserPlanCreateInput; userId?: string } }): Promise<UserPlanType> {
  const url = `${getApiBase()}/api/user-plans`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create UserPlan')
  }
  const json = await response.json()
  return json.data as UserPlanType
}

/**
 * Update an existing UserPlan
 */
export async function updateUserPlan(args: { data: { id: string; input: UserPlanUpdateInput; userId?: string } }): Promise<UserPlanType> {
  const url = `${getApiBase()}/api/user-plans/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update UserPlan')
  }
  const json = await response.json()
  return json.data as UserPlanType
}

/**
 * Delete a UserPlan
 */
export async function deleteUserPlan(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/user-plans/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete UserPlan')
  }
  return { success: true }
}
